apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scale-replica.fullname" . }}-test-connection"
  labels:
    {{- include "scale-replica.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scale-replica.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
